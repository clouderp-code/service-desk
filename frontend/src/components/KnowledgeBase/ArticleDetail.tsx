import React from 'react';
import { Box } from '@mui/material';

const ArticleDetail: React.FC = () => {
  return (
    <Box>
      {/* ArticleDetail implementation */}
    </Box>
  );
};

export default ArticleDetail;
